#ifndef MAIN_H
#define MAIN_H

/**
 * DESC: Header file containing declarations for all functions
 * used in the 0x04 directory
 */

int _isupper(int c);
int _isdigit(int c);
int mul(int a, int b);

#endif
