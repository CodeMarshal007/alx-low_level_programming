#ifndef MAIN_H
#define MAIN_H

/**
 * DESC: Header file containing declarations for all functions
 * used in the 0x05 directory
 */

int _putchar(char c);
void reset_to_98(int *n);
void swap_int(int *a, int *b);

#endif

